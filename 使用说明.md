# 以后怎么添加新的练习链接（再也不用改网页代码了）

> 最近一次改动：删除了「中文（小学·欧洲版）」和「华文（小学·北京华文学院版）」
> 两个版块（连同 109 个链接），现在网站上还剩 **4 本教材、128 个练习链接**。
> 被删的内容已备份在 `_已删除版块备份/` 文件夹里，随时可以恢复。

---

## 一、网站现在的结构

**以前**：教材、册、单元、课、所有链接全部写死在 `index.html` 一个文件里。
所以每加一个链接，就得让 AI 重新输出一整份网页代码。

**现在**：拆成 4 个文件，各管各的。

| 文件 | 作用 | 你要不要改 |
|------|------|-----------|
| `index.html` | 网页外壳（样式、搜索、筛选、渲染） | ❌ 换过一次，以后不用动 |
| `data.js` | 教材目录：哪本书 → 哪一册 → 哪个单元 → 哪几课 | ❌ 基本不用动 |
| `links.js` | **所有练习题链接** | ✅ **你以后只改这一个文件** |
| `add.html` | 「添加链接向导」：帮你生成要粘贴的那一行 | ❌ 只用来点，不用改 |

**目前网站上的 4 本教材**：

| 教材 | 册数 | 课数 | 链接数 |
|------|------|------|--------|
| 中文（小学·暨南大学版） | 6 | 72 | 34 |
| YCT标准教程 | 6 | 72 | 70 |
| 幼儿汉语 | 3 | 30 | 24 |
| 汉语拼音 | 1 | 18 | 0 |

---

## 二、日常添加一条链接（3 步，1 分钟）

### 第 1 步：打开向导

打开网站，点右下角绿色的 **「＋ 添加链接」** 按钮。
（也可以直接打开 `add.html`，或者 `https://你的网址/add.html`）

### 第 2 步：选一选、填一填

1. **教材** → **册** → **单元** → **课**（下拉框里会显示每课已有几个链接）
2. 填 **链接名称**（网页上显示的文字，例如「听力练习3」）
3. 粘贴 **网址**（贴上去后「类型」会自动识别成 Wordwall / Quizlet / Blooket…）
4. 点 **「生成代码」**

下面会立刻出现一行文字，例如：

```
YCT标准教程|第二册|全册12课|第八课|听力练习3|Wordwall|https://wordwall.net/resource/12345678
```

点 **「复制」**。

### 第 3 步：粘贴到 links.js

1. 打开 GitHub 上的 `links.js`
2. 点右上角 **铅笔 ✏️ 图标**（Edit this file）
3. 拉到文件最下面，找到这段：

```
/* ============================================================
   ★★★  新链接加在下面这一行下面就行  ★★★
   ============================================================ */
```

4. 在它下面**另起一行**，把刚才复制的那行粘贴进去
5. 点 **Commit changes**（提交）

搞定。刷新网站就能看到新链接了。✅

---

## 三、一次加多条（比如一个单元新做了 6 个游戏）

在向导页面往下拉，展开 **「一次添加多条（可选）」**：

- 先在①里把教材 / 册 / 单元 / 课 选好
- 在框里每行写一条：`链接名称 | 网址`

```
听力练习1 | https://wordwall.net/resource/111111
听力练习2 | https://wordwall.net/resource/222222
句子练习   | https://wordwall.net/resource/333333
```

- 点 **「生成本批代码」** → 复制 → 粘贴到 `links.js` 最后

---

## 四、手动添加（不用向导）

`links.js` 里每行的格式是：

```
教材 | 册 | 单元 | 课 | 链接名称 | 类型 | 网址
```

举例：

```
中文（小学·暨南大学版）|第四册|第一单元|第一课|词语练习|Quizlet|https://quizlet.com/ie/1234567/
```

**「教材」这一栏的写法很宽松**，写全名或只写关键字都行，下面几种都能认出来：

```
中文（小学·暨南大学版）| …     ✅ 全名
暨南| …                       ✅ 关键字
YCT| …                        ✅ 关键字
拼音| …                       ✅ 关键字
```

**「类型」这一栏可以不写**，系统会根据网址自动判断。所以下面两种写法效果一样：

```
YCT标准教程|第二册|全册12课|第八课|听力练习3|Wordwall|https://wordwall.net/resource/12345678
YCT标准教程|第二册|全册12课|第八课|听力练习3|https://wordwall.net/resource/12345678
```

以 `//` 开头的行会被当成说明文字忽略掉，可以放心写注释。

---

## 五、几种常见情况

### 1. 加的是**全新的一课**（`data.js` 里还没有这一课）

在向导第①步，**「课」** 下拉框拉到最底下选 **「＋ 新建一课…」**，
然后填「课名」和「课文名 / 主题」，再填链接。会生成 **两行**：

```
YCT标准教程|第二册|全册12课|第九课|@主题|课文-我最喜欢的动物
YCT标准教程|第二册|全册12课|第九课|听力练习1|Wordwall|https://wordwall.net/resource/55555555
```

两行一起复制粘贴就行，网页会自动把这一课建出来。

### 2. 想给某一课补上「课文名 / 主题」

在向导第①步选好那一课，在 **「课文名 / 主题」** 栏里填上，再点生成。

### 3. 想改链接的名字或网址

直接在 `links.js` 里改那一行，保存提交就行。

### 4. 想删除一个链接

把 `links.js` 里那一行整行删掉。

### 5. 想调整链接在网页上的顺序

链接在网页上的显示顺序 = 它在 `links.js` 里的先后顺序。
想让它排前面，就把那一行往上挪。

---

## 六、出问题了怎么查

1. **打开 `add.html`，拉到最下面「数据检查」**：
   - 会告诉你一共有多少教材、多少课、多少条链接
   - 如果 `links.js` 里有哪一行写错了，会直接标出**行号**和内容
2. 在网站首页按 `F12` 打开控制台（Console），也会打印同样的提示。

常见错误：
- 分隔符写成了中文的 `｜`？ —— **没关系，中文竖线也认**
- 课名写错了一个字？ —— 会被当成新课新建出来（检查一下拼写）
- 网址不是 `http` 开头？ —— 那一行会被跳过，控制台会提示
- 教材名写成了已经删掉的「欧洲版」「华文」？ —— 会提示「找不到教材」

---

## 七、怎么把这个版本传上去

在 GitHub 仓库 `TanyaLou/chinese-games` 里：

1. 上传 **4 个文件**到仓库根目录：`index.html`、`data.js`、`links.js`、`add.html`
   - 4 个都是覆盖/新增，全部传上去
2. Commit changes
3. 等 1 分钟左右（GitHub Pages 自动部署），刷新网站检查

> ⚠️ 4 个文件必须在**同一个文件夹**里，`index.html` 才能读到另外两个数据文件。

**备份**：
- 改造前的原版单文件网页 → `_原始网页备份/index_original.html`
- 这次删掉的教材和链接 → `_已删除版块备份/`（见下面第八节）

---

## 八、想恢复被删掉的两个版块

被删的内容都在 `_已删除版块备份/` 里：

- `data_已删除的教材目录.js` —— 那两本教材的目录结构
- `links_已删除的链接.js` —— 那两本的 109 条链接

**恢复方法**：

1. 把 `data_已删除的教材目录.js` 里方括号中的内容，整段粘到 `data.js` 的
   `TEXTBOOKS = [ ... ]` 里面（注意前一项末尾补一个逗号）
2. 把 `links_已删除的链接.js` 里的行，粘回 `links.js` 最下面的添加区
3. 提交 → 版块就回来了

> 这次我把 `links.js` 的「教材」栏从数字代号改成了教材名称，
> 就是为了防止以后再有增删时编号错位、链接跑到别的教材下面去。
> 所以备份里的链接也是用名称写的，直接粘回去就能用。

---

## 九、关于在国外能不能用

- 网站托管在 **GitHub Pages**，在爱尔兰、中国、其他地区都能正常打开。
- 网页里引用的 Wordwall、Quizlet、Blooket、北京语言大学书写练习平台，
  也都是全球可访问的服务，从爱尔兰打开没问题。
- 唯一注意点：`index.html` 用了 Google Fonts 和 Font Awesome 的 CDN。
  如果哪天某个地区加载慢，网页仍能正常显示，只是字体/图标会退化成默认样式。
  需要的话可以让我把这两个库改成本地文件，就完全不依赖外部 CDN 了。

---

## 十、一句话总结

> **以后加链接：打开 `add.html` → 点几下 → 复制一行 → 粘贴到 `links.js` 最后 → 提交。**
>
> `index.html` 再也不用重新生成了。
